# Philippine Food-Security Forecasting — Streamlit Dashboard
# Minimal dependencies for dashboard.py only.
# The dashboard reads pre-computed CSV/HTML files — no ML packages needed.
pandas>=2.1
numpy>=1.26
plotly>=5.14
streamlit>=1.35
